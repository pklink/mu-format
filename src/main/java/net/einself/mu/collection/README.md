# collection

Collection root discovery, format version validation, and advisory write lock.

## Dependencies

- shared

## SPEC.md references

- Section 2 — root directory containing `meta/.mu`
- Section 3.2 — store layout
- Section 4.0 — collection marker, format version
- Section 6 — git integration, lock not versioned