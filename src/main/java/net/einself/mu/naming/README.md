# naming

Unicode NFC normalization, filesystem-safe name construction, and file extension derivation
for blob rendering hints.

## Dependencies

- shared

## SPEC.md references

- Section 4.1 — identifier rules (length, no separators, no control chars, no leading/trailing space/dot)
- Section 4.3 — NFC normalization of string values
- Section 4.5 — extension as a rendering hint
- Section 4.9 — origin path NFC normalization
- Section 5.2 — filesystem name construction